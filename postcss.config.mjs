// This project uses Tailwind CSS via CDN (in index.html), so no PostCSS plugins are needed.
// This file exists to prevent Vite from picking up a postcss.config.mjs in a parent directory.
export default {
  plugins: {},
};
